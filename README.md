## The DSA Scrapbook

A collection of solutions to problems I've solved (or attempted to solve) on `LeetCode`, `CodeForces`, and `CSES`.

This is mostly a local scratchpad that occasionally escapes onto GitHub. Expect inconsistent updates, half-forgotten solutions, and the occasional piece of code that I swear made sense at the time.

Basically: if it's here, I probably solved it. Eventually.
